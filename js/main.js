;(function(w,d){
	var body = d.body,
	query = d.querySelector.bind(d),
	queryAll = d.querySelectorAll.bind(d),
	root = query('html'),
	gotop = query('#gotop'),
	menu = query('#menu'),
	header = query('#header'),
	mask = query('#mask'),
	menuToggle = query('#menu-toggle'),
	menuOff = $('#menu-off'),
	loading = $('#loading'),
	animate = w.requestAnimationFrame,
	scrollSpeed = 200 / (1000 / 60);


})(window,document);